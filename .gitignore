# Dependencies
/node_modules

# Build output — built fresh by the release workflow and shipped as a release asset
dist/
GEMINI.md

# Lockfiles for other package managers
bun.lock
yarn.lock

.DS_Store

venv/

test-env/config/*
!test-env/config/configuration.yaml
!test-env/config/ui-lovelace.yaml

# Playwright
test-results/
playwright-report/

# Local machine compose override (corporate CA, local paths)
test-env/docker-compose.override.yml
